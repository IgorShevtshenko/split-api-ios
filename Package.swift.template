// swift-tools-version:5.10
import PackageDescription

let package = Package(
    name: "SplitApi",
    platforms: [.iOS(.v15)],
    products: [
        .library(
            name: "SplitApi",
            targets: ["SplitApi"]
        ),
    ],
    targets: [
        .binaryTarget(
            name: "SplitApi",
            url: "https://github.com/IgorShevtshenko/split-api/releases/download/{{VERSION}}/splitapi.xcframework.zip",
            checksum: "{{CHECKSUM}}"
        ),
    ]
)